# react-native-demo

To install this project run command
npm install
npx pod-install or cd ios && pod install && cd ..
npx react-native link

To run this project run command
npx react-native run-ios
npx react-native run-android

